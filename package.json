{
  "name": "tourcoaster-site",
  "private": true,
  "scripts": {
    "prefetch": "node scripts/prefetch-discovery.mjs",
    "build": "node scripts/prefetch-discovery.mjs && PREFETCH_DISCOVERY=0 bundle exec jekyll build",
    "serve": "node scripts/prefetch-discovery.mjs && PREFETCH_DISCOVERY=0 bundle exec jekyll serve --host 0.0.0.0 --port 5000"
  }
}
